# NOTES_tpex_endpoint.md — TPEx 上櫃外資買賣超端點驗證紀錄

> 對應 SPEC_tpex_spot.md Phase 0。
> 狀態：**已於 2026-07-14 透過 Actions `inspect-tpex` 實測**（原始回應摘要見 §5.1）。
> 三陷阱結論：日期＝民國 `YYY/MM/DD`、單位＝元、外資取「外資及陸資合計」列。
> 剩餘待辦：人類版頁面同日目視複核（3 個交易日）與歷史日期查詢確認（§5）。

## 0. 開發環境限制聲明（誠實記錄）

本次開發所在的 CI sandbox 網路政策**封鎖所有資料來源主機**
（`www.tpex.org.tw` / `www.twse.com.tw` / `www.taifex.com.tw` / `api.finmindtrade.com`
一律 CONNECT 403，含現行程式既有的 TWSE/TAIFEX 端點）。
因此 Phase 0 的「傾印原始 response、與人類版頁面逐欄核對」無法在開發環境完成，
已改為：

1. 驗證步驟**完整寫成 `--inspect-tpex YYYYMMDD`**（比照現有 `--inspect`），
   並接入 `.github/workflows/tools.yml` 的 `inspect-tpex` 模式。
2. 解析器 `_parse_tpex_insti()` 以**欄位關鍵字比對**（比照 `find_col()` 慣例）
   而非固定索引撰寫，容忍欄位順序差異與兩種 JSON 外形（`tables[]` 與頂層 `data/aaData`）。
3. 單位換算獨立成常數 `TPEX_AMOUNT_MULT`（預設 1.0），實測若為千元欄只需改一處。

**首次上線前必跑**：Actions → 工具 → Run workflow → mode=`inspect-tpex`、
start=最近交易日，依 §5 檢核清單核對後把結論回填本文件。

## 1. 候選來源評估

| # | 候選 | 評估結果 |
|---|---|---|
| A | TPEx 新版網站 JSON 端點 | **採用**。與現行 TWSE RWD 用法最接近；同一端點以 `date` 參數查歷史日期 → backfill 可用同一來源 |
| B | TPEx OpenAPI（`/openapi/`） | 不採用主線。多數 dataset 為「最新一日」快照，不支援歷史日期查詢 → backfill 不可用（SPEC §9 已知風險）。保留為每日增量的備援選項 |
| C | FinMind | 不採用。不引入新依賴/金鑰管理；市場層級彙總 dataset 的存在性與延遲需另行確認 |

## 2. 採用端點與參數（候選 A）

- 端點常數：`TPEX_INSTI_URL = "https://www.tpex.org.tw/www/zh-tw/insti/summary"`
  （檔頭設定區，與其他 `*_URL` 併列）
- 對應人類版查詢頁：櫃買中心網站 → 上櫃 → 三大法人 → 買賣金額彙總
  （`https://www.tpex.org.tw/zh-tw/mainboard/trading/major-institutional/summary/day.html`）
- 參數範例（`http_get`，GET；**2026-07-14 實測確認**）：

```
type=Daily
date=115/07/14        ← 民國 YYY/MM/DD（由 _to_tpex_date() 轉換）
response=json
```

- 程式行為（依實測修正）：**只用民國格式**。實測西元 `2026/07/14` 會先回
  520、重試後回應的資料日仍是 `115/07/14` —— 無法排除伺服器「忽略看不懂的
  日期參數、直接回最新一日」的行為，對 backfill 是資料錯置風險，故不做
  西元 fallback。另加**回應資料日＝查詢日**驗證（`_tpex_resp_date_iso()`）：
  不一致（假日 / 尚未公布時伺服器回最新一日）一律視為無資料回 `None`，
  防止把別天的數字存到錯的日期。
- 抓取函式：`fetch_spot_net_tpex(date_yyyymmdd) -> float | None`，
  介面與回傳語意完全比照 `fetch_spot_net()`。

## 3. 欄位對照表（**2026-07-14 實測確認**）

實際回應外形（top-level keys：`tables` / `template` / `date` / `stat`）：

```json
{
  "date": "115/07/14", "stat": "ok",
  "tables": [
    {
      "title": "三大法人買賣金額彙總表",
      "date": "115/07/14",
      "fields": ["單位名稱", "買進金額(元)", "賣出金額(元)", "買賣超(元)"],
      "data": [
        ["外資及陸資合計", "104,666,912,656", "90,891,247,315", "13,775,665,341"],
        ["　外資及陸資(不含自營商)", "…", "…", "…"],
        ["　外資自營商", "0", "0", "0"],
        ["投信", "…"], ["自營商合計", "…"], ["…"],
        ["三大法人合計*", "…"]
      ]
    }
  ]
}
```

| 標準鍵 | TPEx 實際欄位 | 定位方式 |
|---|---|---|
| 外資列 | `外資及陸資合計`（另有 `　外資及陸資(不含自營商)`、`　外資自營商` 兩子列，前綴全形空白） | 列名含「外資」關鍵字 |
| 買賣差額 | `買賣超(元)`（索引 3；TPEx 用「買賣超」而非 TWSE 的「買賣差額」） | `fields` 含「差額」**或「買賣超」**；找不到退最後一欄 |
| 合計去重 | 直接取「外資及陸資合計」列 | 外資列中存在含「合計」列 → 只取合計列，避免與子項重複加總（實測驗證：合計 13,775,665,341 = 子項相加） |

加總口徑對齊現行 `fetch_spot_net()`：**外資及陸資 + 外資自營商 之買賣差額合計**
（＝「外資及陸資合計」列，實測一致）。

## 4. 三大陷阱結論（**2026-07-14 實測定案**）

| 陷阱 | 結論 | 依據 |
|---|---|---|
| **日期格式** | ✅ 參數採民國 `YYY/MM/DD`（`_to_tpex_date('20260714')` → `'115/07/14'`）；**不做西元 fallback**；response 內日期亦為民國 `115/07/14` | `date=115/07/14` 直接回資料；`date=2026/07/14` 先 520、重試後資料日仍為 `115/07/14`（疑似忽略參數回最新一日）→ 改為單一格式 + 回應日驗證 |
| **金額單位** | ✅ **元**，`TPEX_AMOUNT_MULT = 1.0` 維持不變 | `fields` 欄名明示：`買進金額(元)` / `賣出金額(元)` / `買賣超(元)`（TWSE BFI82U 同為元；TAIFEX 才是千元欄） |
| **外資分類欄位** | ✅ 列名為「外資及陸資合計」＋「　外資及陸資(不含自營商)」＋「　外資自營商」（子列前綴全形空白）；差額欄名為「買賣超(元)」非「買賣差額」 | 解析器取「合計」列去重；欄位比對已加「買賣超」關鍵字。實測外資合計 13,775,665,341 元 |

## 5. 上線前核對清單

- [x] 記錄實際被接受的日期參數格式：**民國 `YYY/MM/DD`**；response 內日期欄
      同為民國 `115/07/14`（2026-07-14 Actions `inspect-tpex` 實測）
- [x] 傾印的 `fields` 與 §3 對照表一致（已依實況更新：差額欄名為「買賣超(元)」）
- [x] 單位結論回填 §4：**元**，`TPEX_AMOUNT_MULT = 1.0` 維持
- [ ] `_parse_tpex_insti` 外資合計與人類版頁面**至少 3 個不同交易日誤差 = 0**
      （2026-07-14 端點值已取得，待人類版頁面目視複核＋另 2 個交易日）
- [ ] 用歷史日期（如 20 個交易日前）跑一次 `inspect-tpex` 確認同端點可查
      歷史日期（回應資料日＝該歷史日）→ backfill 可行性定案

### 5.1 核對數字對照（回填區）

| 日期 | 端點外資合計（元） | 人類版頁面合計（元） | 誤差 |
|---|---|---|---|
| 2026-07-14 | 13,775,665,341（外資及陸資合計；子列 13,775,665,341 + 0） | （待目視複核） | — |
| （待填） | | | |
| （待填） | | | |

原始回應摘要（2026-07-14，`date=115/07/14`）：
`fields = ['單位名稱', '買進金額(元)', '賣出金額(元)', '買賣超(元)']`；
外資三列 = 合計 13,775,665,341 ／ 不含自營商 13,775,665,341 ／ 外資自營商 0。
此回應已固化為離線解析測試的樣本資料。

## 6. 公布時間與缺值行為

- TPEx 約每交易日 15:00 後公布，與 TWSE 可能有時間差。
- 單邊缺值**不阻擋當日入庫**：`spot_net_tpex` 不參與「spot/tx/opt 全 None 才跳過」
  判斷；當日缺值存 `None`，前端顯示 `--`，隔日重抓由既有同日 upsert 機制補齊。
- 缺值當日 job 以 exit code 0 結束，僅 `log()` 警示（已驗證：`fetch_spot_net_tpex`
  失敗路徑一律回 `None`，不 raise）。
